--Rectangle select cursor
Replace floodfill by: Make line around sprite, then scanlinefill
Checkbox, radiobutton

Manažment lokálnosti pre radiobutton apod, prípadne keypress

#pragma once

class ElementController {
	int selectedID; //is modified if control has select event
};


class GlobalInfo {
public:
	int SelectedID;
}

class ElementGroup {
	int GroupID;
	inf x,y;
	vector<GuiElement*> elements;

}

class GuiElement {
protected:
	int x,y;
	bool visible;
	int ID;
	ElementGroup* parent = nullptr; //podla group offsetni suradnice pri kresleni gui
	ElementInfo* LocalInfo = this;
	virtual void Draw() = 0;
public:
	static SelectedElement

	bool Selected
}

class GuiElement {
public:
	GlobalInfo* info;
}

class GroupBox {
  implement
};

class 