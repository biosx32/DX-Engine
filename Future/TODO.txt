#Cube selector - size and position for easily adjusting elements
#update mousehelper a podobne data v Windows.cpp
Broken checkbox position inheritenace and so...
#physics object shape...

Different Scrollbar design

rename interface to Graphics destination or sth like that
ISSUE - PRESS ONLY WORKING IF MHELPER REFRESH AFTER ALL IS REFRESHED!!! (maybe need to change mhelper refresh line order)
Stack object are added into vector before vector is defined = all stack members are cleared on initialization